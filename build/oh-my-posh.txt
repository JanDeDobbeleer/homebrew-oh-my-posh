class OhMyPosh < Formula
  desc "Prompt theme engine for any shell"
  homepage "https://ohmyposh.dev"
  url "https://github.com/JanDeDobbeleer/oh-my-posh3/archive/v<VERSION>.tar.gz"
  sha256 "<SHA256>"
  license "GPL-3.0-only"
  version "<VERSION>"

  depends_on "go" => :build
  depends_on "go-bindata" => :build

  def install
    system "cd", "src/"
    system "go", "generate"
    system "go", "build", "-o", "oh-my-posh", "-ldflags=\"-X 'main.Version=<VERSION>'\""
    bin.install "oh-my-posh"
    mv "themes", prefix
  end

  def caveats
    <<~EOS
      Thanks for installing Oh my Posh.
      Have a look at https://ohmyposh.dev/docs/installation for detailed instructions for your shell.
      Sample themes can be found at $(brew --prefix oh-my-posh)/themes.
    EOS
  end

  test do
    system "#{bin}/oh-my-posh", "--help"
  end
end
